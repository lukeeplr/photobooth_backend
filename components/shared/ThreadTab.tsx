import React from 'react'

type ThreadTypeProps = {
    currentUserId: string
    accountId: string
    accountType: string
}

export default function ThreadTab({currentUserId, accountId, accountType}: ThreadTypeProps) {
  return (

    //TODO: Fetch threads do usuario
    <section>
        ThreadTab
    </section>
  )
}
